{
  "name": "sommerprojekt-wmc-fooding",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --prefix backend/server\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend/server && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
